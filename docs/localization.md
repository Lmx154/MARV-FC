This is the document where the localization architecure is specified. 

The localization architecuture is used within the direct compiled firmware that flashed directly to the hardware. The simulator also utilizes this localization architecture. This is so we can re-use code, but more importantly it allows us to maintain the same logic within the simulation and the control loop. 

