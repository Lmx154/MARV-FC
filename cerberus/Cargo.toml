[package]
name = "cerberus"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4", default-features = true, features = ["clock"] }
eframe = "0.33"
serialport = { version = "4", default-features = false }
