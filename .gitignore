/target
telemetry-app/gazebo_bridge/build/

# Treat `mavlink/` as a source folder for the dialect XML only.
# Any generated bindings/artifacts should live under `mavlink/` too, but be ignored.
mavlink/**
!mavlink/marv_fc.xml
